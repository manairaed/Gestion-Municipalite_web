
{% extends 'baseback.html.twig' %}


{% block body %}
<br>
<br><br>
<center>
    <h1>Vehicule</h1>
</center>
<br><br>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vehicule.id }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ vehicule.marque }}</td>
            </tr>
            <tr>
                <th>Disponible</th>
                <td>{{ vehicule.disponible ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-sm btn-success" href="{{ path('app_vehicule_index') }}">Retour à la liste</a>

    <a class="btn btn-round btn-facebook" href="{{ path('app_vehicule_edit', {'id': vehicule.id}) }}">Modifier</a>

    {{ include('vehicule/_delete_form.html.twig') }}
{% endblock %}
